/*--------------------------------------------------------------------------------------
 *  Placeholder — Void accept/reject bar lives in the editor command bar (VoidCommandBar).
 *  Avoid importing void/react/src here; that breaks workbench ESM bootstrap.
 *--------------------------------------------------------------------------------------*/

import React from 'react';

export function AgenticVoidCommandBar() {
	return null;
}
